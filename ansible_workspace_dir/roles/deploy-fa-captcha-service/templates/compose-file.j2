version: '3'

services:
  {{ docker_service_name }}:
    image: {{ docker_hub_url }}/{{ image_name }}:{{ tag }}
    hostname: captcha
    restart: unless-stopped
    ports:
      - {{ host_port }}:{{ docker_port }}